{
  "name": "cirrus",
  "version": "1.0.1",
  "scripts": {
    "build": "npm run build:browser && npm run build:test",
    "build:browser": "vite build",
    "build:test": "vite build --mode test",
    "dev": "vite",
    "lint": "jshint libs/*.js libs/entries/*.js libs/fake_vjavascript/*.js",
    "test": "jasmine-node spec"
  },
  "dependencies": {
    "jsonfile": "0.0.1"
  },
  "devDependencies": {
    "benchmark": "1.0.0",
    "handlebars": "4.7.7",
    "jasmine-node": "^1.16.2",
    "jshint": "^2.13.6",
    "vite": "^5.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
